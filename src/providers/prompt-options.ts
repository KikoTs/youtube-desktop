import youtubeMusicTrayIcon from '@assets/youtube-tray.png?asset&asarUnpack';

const promptOptions = {
  customStylesheet: 'dark',
  icon: youtubeMusicTrayIcon,
};

export default () => promptOptions;
